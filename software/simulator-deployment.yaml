apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f compose.yaml
    kompose.version: 1.32.0 (HEAD)
  labels:
    io.kompose.service: simulator
  name: simulator
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: simulator
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f compose.yaml
        kompose.version: 1.32.0 (HEAD)
      labels:
        io.kompose.network/incubatornet: "true"
        io.kompose.service: simulator
    spec:
      containers:
        - image: localhost:5050/incubator-simulator:latest
          name: simulator
          ports:
            - containerPort: 1889
              protocol: TCP
      restartPolicy: Always
